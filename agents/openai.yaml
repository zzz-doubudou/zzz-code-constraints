interface:
  display_name: "Concise Code Constraints"
  short_description: "Apply focused code design and review constraints"
  default_prompt: "Use $zzz-code-constraints when this task needs focused scope, method extraction, call-chain, comment, or risk-based validation rules."
